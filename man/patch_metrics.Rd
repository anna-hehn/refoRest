% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/patch_metrics.R
\name{patch_metrics}
\alias{patch_metrics}
\title{Compute patch metrics from a disturbance mask}
\usage{
patch_metrics(
  disturbance,
  severity = NULL,
  first_idx = NULL,
  directions = 8,
  min_patch_cells = 1,
  return_polygons = TRUE,
  stats_severity = c("mean", "min", "max"),
  stats_first_idx = c("min")
)
}
\arguments{
\item{disturbance}{SpatRaster. Disturbance mask (1 for disturbed, NA/0 otherwise).
Usually the \verb{$disturbance} output from \code{detect_disturbance_z()}.}

\item{severity}{Optional SpatRaster. Per-pixel severity layer (e.g. \verb{$severity} from \code{detect_disturbance_z()}).}

\item{first_idx}{Optional SpatRaster. Per-pixel first detection index (e.g. \verb{$first_idx} from \code{detect_disturbance_z()}).}

\item{directions}{Integer. Neighborhood connectivity for patch labelling:
4 (rook) or 8 (queen). Default 8.}

\item{min_patch_cells}{Integer >= 1. Patches smaller than this number of cells are removed.}

\item{return_polygons}{Logical. If TRUE, returns dissolved patch polygons.}

\item{stats_severity}{Character vector. Patch stats for severity. Default c("mean","min","max").}

\item{stats_first_idx}{Character vector. Patch stats for first_idx. Default c("min").}
}
\value{
A list with:
\describe{
\item{patch_id}{SpatRaster with patch IDs (NA outside patches).}
\item{patch_table}{data.frame with patch metrics (one row per patch).}
\item{patch_polygons}{SpatVector polygons (if return_polygons = TRUE), otherwise NULL.}
}
}
\description{
Takes a binary disturbance raster (typically 1 = disturbed, NA or 0 = not disturbed),
labels connected disturbed pixels into patches, and computes patch-level metrics
such as area (and optionally perimeter/shape metrics). Optionally aggregates
severity and/or first occurrence layers per patch.
}
