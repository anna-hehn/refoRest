% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect_disturbance_z.R
\name{detect_disturbance_z}
\alias{detect_disturbance_z}
\title{Detect disturbance from an index time series using (robust) z-scores}
\usage{
detect_disturbance_z(
  x,
  baseline = 1:5,
  z_thresh = -2,
  direction = c("drop", "rise"),
  min_duration = 1,
  robust = TRUE,
  min_spread = 0
)
}
\arguments{
\item{x}{terra SpatRaster time series (layers named as dates, or any names)}

\item{baseline}{integer vector of layer indices used as reference period}

\item{z_thresh}{numeric threshold (e.g. -2 for drops). More extreme = stricter.}

\item{direction}{"drop" (default) or "rise"}

\item{min_duration}{integer, how many consecutive layers must meet the threshold}

\item{robust}{logical, if TRUE uses median/MAD instead of mean/sd (more robust to outliers)}

\item{min_spread}{numeric >= 0. Optional lower bound for baseline spread (MAD/SD).
Use >0 to avoid extremely large absolute z-scores when baseline variability is tiny.}
}
\value{
list(disturbance, first_idx, severity, summary)
}
\description{
Detect disturbance from an index time series using (robust) z-scores
}
